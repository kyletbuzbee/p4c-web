# Frontend Environment Variables
# Copy this file to .env and configure the values for your environment

# =============================================================================
# API Configuration for Future Backend Integration
# =============================================================================

# Backend API Base URL
# Used to connect to your backend server when deployed
#
# Development (Mock Data):
# Leave empty or unset to use mock data during development
# Example: VITE_API_URL=
#
# Production (Real Backend):
# Set to your actual backend API endpoint
# Example: VITE_API_URL=https://api.yourdomain.com
# Example: VITE_API_URL=http://localhost:3001/api
# Corrected .env.local
VITE_API_URL=http://localhost:3001/api
VITE_REPOSITORY_NAME=p4c-web

# =============================================================================
# Development vs Production Setup
# =============================================================================

# Development Mode (Default):
# - Uses mock data from services/api.ts
# - Simulates network latency for realistic UX
# - No backend connection required
# - Perfect for frontend development and testing

# Production Mode:
# - Set VITE_API_URL to your backend endpoint
# - API calls will be made to the real backend
# - Mock data will be bypassed automatically
# - Requires backend server to be running

# =============================================================================
# Backend Integration Instructions
# =============================================================================

# To enable real backend integration:
# 1. Set VITE_API_URL in your .env file
# 2. Ensure your backend exposes the expected endpoints:
#    - GET /api/impact/metrics
#    - GET /api/impact/financial-breakdown
#    - GET /api/transparency/standards
#    - GET /api/properties
#    - GET /api/properties/:id
#    - POST /api/properties/search
#    - GET /api/properties/badge/:badge
# 3. Update your backend to return the same data structure as mock data
# 4. Deploy both frontend and backend

# =============================================================================
# Environment-Specific Examples
# =============================================================================

# Local Development (with local backend):
# VITE_API_URL=http://localhost:3001/api

# Staging Environment:
# VITE_API_URL=https://staging-api.yourdomain.com

# Production Environment:
# VITE_API_URL=https://api.yourdomain.com

# Docker Development:
# VITE_API_URL=http://backend:3001/api

# =============================================================================
# Security Notes
# =============================================================================

# - Never commit your actual .env file to version control
# - Use different URLs for different environments
# - Ensure your backend has proper CORS configuration
# - Consider using environment-specific backend URLs
# - Add authentication tokens if required by your backend