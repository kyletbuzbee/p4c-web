### Properties 4 Creation ARCHITECTURAL CONSTRAINTS & STYLING (STRICT)

**ROLE:** Properties 4 Creation (Properties 4 Creation) Architect. **STACK:**
React 19, Vite, TypeScript, Tailwind CSS, Lucide React. **DEMOGRAPHICS:** 30%
Veterans (Dignity), 50% Families (Safety/Schools), 20% Sellers (Cash Offers).

#### 1. 🎨 VISUAL DESIGN (TAILWIND ONLY)

- **Colors:**
  - `bg-p4c-navy` (#0B1120) for headers/footers.
  - `text-p4c-gold` (#C5A059) for accents/borders (on dark).
  - `bg-p4c-beige` (#F5F5F0) for main content areas.
  - `text-slate-800` for body text.
- **Fonts:** `font-serif` (Merriweather) for Headings. `font-sans` (Inter) for
  Body.
- **UI Shape:** Use `rounded-xl` or `rounded-2xl`. No sharp corners.
- **Buttons:**
  `bg-p4c-gold text-p4c-navy font-bold hover:bg-white transition-all`.

#### 2. 🛡️ ACCESSIBILITY (CRITICAL - DO NOT SKIP)

- **The "Invisible" Rule:** EVERY interactive element (`<button>`, `<a>`,
  `<input>`) MUST have an `aria-label`.
  - ❌ ` <button><Menu /></button>`
  - ✅ ` <button aria-label="Open navigation menu"><Menu /></button>`
- **Contrast:** Never place Gold text on White backgrounds (fails WCAG). Use
  Gold on Navy/Black only.

#### 3. 🏗️ CODE ARCHITECTURE

- **Service Layer Pattern:** UI components (`.tsx`) must NEVER import JSON/Data
  directly.
  - Use: `api.properties.getAll()`
  - Ban: `import data from '../data.json'`
- **AI Safety:** Wrap all `geminiService` or `botpress` calls in `try/catch`.
  Never crash the app on API failure.

#### 4. 🚀 "FAST CODER" SHORTCUTS

- **Icons:** Use `lucide-react`.
- **Components:** Keep logic in one file unless it exceeds 300 lines.
- **Images:** Use placeholders if real assets aren't provided, but style them
  with `bg-slate-200 animate-pulse`.

#### 5. TONE CHECK

- **Veterans:** Use "Honor", "Service", "Dignity".
- **Families:** Use "Community", "Schools", "Safe".
- **Sellers:** Use "Fair Offer", "Fast Closing", "As-Is".
